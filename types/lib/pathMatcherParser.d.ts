/**
 * Check if there is a white space
 * @private
 * @param {string} c
 * @returns {boolean}
 */
export function isWhitespace(c: string): boolean
export default parseIncludes
/**
 * parse and include expression and return a Matcher
 * @param {string|MatcherContainer?} str - the include expression
 * @return {MatcherContainer}
 */
declare function parseIncludes(
  str: string | (MatcherContainer | null),
): MatcherContainer
import { MatcherContainer } from "./pathMatcher.js"
//# sourceMappingURL=pathMatcherParser.d.ts.map
